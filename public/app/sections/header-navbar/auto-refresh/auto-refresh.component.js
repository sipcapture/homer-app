import template from './templates/auto-refresh.template.html';
import controller from './controllers/auto-refresh.controller';

const autoRefresh = {
  controller,
  template,
};

export default autoRefresh;
