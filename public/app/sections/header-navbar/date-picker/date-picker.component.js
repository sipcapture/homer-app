import template from './templates/date-picker.template.html';
import controller from './controllers/date-picker.controller';

const datePicker = {
  controller,
  template,
};

export default datePicker;
