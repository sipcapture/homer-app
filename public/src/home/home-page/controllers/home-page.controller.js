const homePage  = function($location, $log, AuthenticationService) {
  const self = this;

  self.$onInit = function() {
  };
  
};

homePage.$inject = ['$location', '$log', 'AuthenticationService'];
export default homePage;
