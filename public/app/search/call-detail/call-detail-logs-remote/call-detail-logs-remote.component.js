import template from './templates/call-detail-logs-remote.template.html';
import controller from './controllers/call-detail-logs-remote.controller';

const callDetailLogsRemote = {
  controller,
  template,
  bindings: {
  },
};

export default callDetailLogsRemote;
