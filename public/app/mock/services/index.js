import angular from 'angular';
import MockAppPreferencesService from './mock-app-preferences-service';

export default angular.module('hepicApp.mock.services', [
  MockAppPreferencesService.name,
]);
