import template from './templates/remote-search.template.html';
import controller from './controllers/remote-search.controller';

const remoteSearch = {
  controller,
  template,
};

export default remoteSearch;
