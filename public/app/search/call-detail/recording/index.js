import angular from 'angular';
import component from './call-detail-recording.component';

export default angular.module('hepicApp.call-detail-recording', []).component('callDetailRecording', component);
